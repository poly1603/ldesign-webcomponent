# 组件测试补充计划

> 创建时间：2025-11-24

## 🎯 测试目标

### 阶段性目标
- **Phase 1**: 验证现有10个测试 ✅
- **Phase 2**: 补充20个核心组件测试 (50% 覆盖率)
- **Phase 3**: 补充剩余20个组件测试 (80% 覆盖率)
- **Phase 4**: E2E和高级测试

---

## 📋 Phase 2: 核心组件测试 (优先级P0-P1)

### 表单类组件 (7个)

#### 1. Input (P0)
**文件**: `src/components/input/input.spec.ts`
**预计用例**: 25+

测试场景：
- 基础渲染和类型（text/password/textarea/number）
- 值绑定和更新
- 事件处理（input/change/focus/blur/clear）
- 前后缀图标
- 清空按钮
- 密码显示切换
- 禁用和只读
- 最大/最小长度
- 自适应高度（textarea）
- 边缘情况

#### 2. Select (基础) (P0)
**文件**: `src/components/select/select.spec.ts`
**预计用例**: 30+

测试场景：
- 基础渲染和选项
- 单选和多选
- 值绑定
- 搜索过滤
- 清空功能
- 禁用选项
- 分组选项
- 自定义触发器
- 下拉面板控制
- 键盘导航
- 边缘情况

#### 3. Checkbox (P0)
**文件**: `src/components/checkbox/checkbox.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 选中状态
- 半选状态
- 值绑定
- 禁用状态
- 边框模式
- 按钮样式
- 事件处理
- CheckboxGroup集成

#### 4. Radio (P0)
**文件**: `src/components/radio/radio.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 选中状态
- 值绑定
- 禁用状态
- 按钮样式
- RadioGroup集成
- 事件处理

#### 5. Switch (P1)
**文件**: `src/components/switch/switch.spec.ts`
**预计用例**: 15+

测试场景：
- 基础渲染
- 开关状态
- 值绑定
- 禁用状态
- 加载状态
- 尺寸
- 自定义文本/图标
- 事件处理

#### 6. DatePicker (P1)
**文件**: `src/components/datepicker/datepicker.spec.ts`
**预计用例**: 35+

测试场景：
- 基础渲染
- 日期选择
- 范围选择
- 月份/年份选择
- 日期格式化
- 禁用日期
- 显示周数
- 快捷选项
- 清空功能
- 事件处理

#### 7. TimePicker (P1)
**文件**: `src/components/time-picker/time-picker.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 时间选择
- 时间范围
- 时间格式
- 步长设置
- 禁用时间
- 清空功能

---

### 反馈类组件 (4个)

#### 8. Modal (P0)
**文件**: `src/components/modal/modal.spec.ts`
**预计用例**: 25+

测试场景：
- 基础渲染
- 显示/隐藏
- 标题和内容
- 底部按钮
- 确认和取消事件
- 遮罩层
- 关闭按钮
- ESC关闭
- 宽度设置
- 嵌套模态框

#### 9. Drawer (P0)
**文件**: `src/components/drawer/drawer.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 四个方向
- 显示/隐藏
- 宽度/高度
- 遮罩层
- 关闭按钮
- 事件处理

#### 10. Message (P1)
**文件**: `src/components/message/message.spec.ts`
**预计用例**: 15+

测试场景：
- 不同类型（success/error/warning/info）
- 显示和关闭
- 持续时间
- 可关闭
- 图标
- 位置

#### 11. Notification (P1)
**文件**: `src/components/notification/notification.spec.ts`
**预计用例**: 20+

测试场景：
- 不同类型
- 显示和关闭
- 标题和描述
- 位置（4个角）
- 持续时间
- 可关闭
- 图标

---

### 数据展示组件 (4个)

#### 12. Table (基础) (P0)
**文件**: `src/components/table/table.spec.ts`
**预计用例**: 30+

测试场景：
- 基础渲染
- 列配置
- 数据绑定
- 排序
- 边框和斑马纹
- 固定列
- 固定表头
- 空数据
- 加载状态
- 虚拟滚动

#### 13. Pagination (P0)
**文件**: `src/components/pagination/pagination.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 页码切换
- 每页数量
- 总数显示
- 快速跳转
- 尺寸
- 简洁模式
- 事件处理

#### 14. Tabs (P1)
**文件**: `src/components/tabs/tabs.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 标签页切换
- 类型（line/card）
- 位置（top/bottom/left/right）
- 新增和删除
- 禁用标签
- 事件处理

#### 15. Collapse (P1)
**文件**: `src/components/collapse/collapse.spec.ts`
**预计用例**: 15+

测试场景：
- 基础渲染
- 展开/收起
- 手风琴模式
- 默认展开
- 禁用面板
- 事件处理

---

### 导航类组件 (3个)

#### 16. Menu (P0)
**文件**: `src/components/menu/menu.spec.ts`
**预计用例**: 25+

测试场景：
- 基础渲染
- 菜单项选中
- 子菜单
- 垂直/水平模式
- 收起模式
- 禁用项
- 分组
- 事件处理

#### 17. Dropdown (P1)
**文件**: `src/components/dropdown/dropdown.spec.ts`
**预计用例**: 20+

测试场景：
- 基础渲染
- 触发方式
- 菜单项
- 位置
- 禁用项
- 分割线
- 事件处理

#### 18. Breadcrumb (P1)
**文件**: `src/components/breadcrumb/breadcrumb.spec.ts`
**预计用例**: 15+

测试场景：
- 基础渲染
- 面包屑项
- 分隔符
- 链接
- 下拉菜单

---

### 通用组件 (2个)

#### 19. Alert (P1)
**文件**: `src/components/alert/alert.spec.ts`
**预计用例**: 15+

测试场景：
- 不同类型
- 标题和描述
- 可关闭
- 图标
- 关闭事件

#### 20. Progress (P1)
**文件**: `src/components/progress/progress.spec.ts`
**预计用例**: 20+

测试场景：
- 线形和圆形
- 进度百分比
- 状态（success/error/warning）
- 颜色
- 文字显示
- 动画

---

## 📊 Phase 3: 补充组件测试 (优先级P2)

### 剩余20个组件（按优先级排序）

21. Upload (P2) - 文件上传
22. Image (P2) - 图片展示
23. Avatar (P2) - 头像
24. Badge (P2) - 徽标
25. Tag (P2) - 标签
26. Card (P2) - 卡片
27. Tooltip (P2) - 文字提示
28. Steps (P2) - 步骤条
29. Slider (P2) - 滑块
30. Rate (P2) - 评分
31. Transfer (P2) - 穿梭框
32. Tree (P2) - 树形控件
33. Cascader (P2) - 级联选择
34. Timeline (P2) - 时间轴
35. Descriptions (P2) - 描述列表
36. Divider (P2) - 分割线
37. Skeleton (P2) - 骨架屏
38. Empty (P2) - 空状态
39. Spin (P2) - 加载中
40. Icon (P2) - 图标

---

## 📝 测试模板

### 基础测试模板

```typescript
import { newSpecPage } from '@stencil/core/testing';
import { ComponentName } from './component-name';

describe('l-component-name', () => {
  describe('基础渲染', () => {
    it('should render with default props', async () => {
      const page = await newSpecPage({
        components: [ComponentName],
        html: `<l-component-name></l-component-name>`,
      });
      expect(page.root).toBeTruthy();
    });

    it('should render with custom content', async () => {
      const page = await newSpecPage({
        components: [ComponentName],
        html: `<l-component-name>Custom Content</l-component-name>`,
      });
      expect(page.root?.textContent).toBe('Custom Content');
    });
  });

  describe('属性测试', () => {
    it('should handle prop changes', async () => {
      const page = await newSpecPage({
        components: [ComponentName],
        html: `<l-component-name prop="value"></l-component-name>`,
      });
      const component = page.rootInstance;
      expect(component.prop).toBe('value');
    });
  });

  describe('事件测试', () => {
    it('should emit events', async () => {
      const page = await newSpecPage({
        components: [ComponentName],
        html: `<l-component-name></l-component-name>`,
      });
      const component = page.rootInstance;
      const eventSpy = jest.fn();
      page.root?.addEventListener('ldesignEvent', eventSpy);
      
      // 触发事件
      await component.someMethod();
      await page.waitForChanges();
      
      expect(eventSpy).toHaveBeenCalled();
    });
  });

  describe('边缘情况', () => {
    it('should handle edge cases', async () => {
      // 测试边缘情况
    });
  });
});
```

---

## 🔄 测试工作流程

### 1. 准备阶段
- 阅读组件源码，理解功能和API
- 确定需要测试的场景
- 创建测试文件

### 2. 编写测试
- 使用测试模板开始
- 按分组编写测试用例
- 确保覆盖所有功能点

### 3. 运行测试
```bash
# 运行特定组件测试
npm test -- component-name

# 监听模式
npm test -- component-name --watch

# 查看覆盖率
npm test -- component-name --coverage
```

### 4. 修复问题
- 根据测试结果修复代码
- 更新测试用例
- 重新运行测试

### 5. 代码审查
- 检查测试质量
- 确保代码覆盖率
- 提交代码

---

## 📈 进度追踪

### 每日目标
- **Day 1**: 验证现有测试，修复依赖
- **Day 2-3**: 完成5个P0组件测试
- **Day 4-5**: 完成剩余5个P0组件测试
- **Day 6-7**: 完成10个P1组件测试
- **Day 8-10**: 完成20个P2组件测试

### 周报模板
```markdown
## 本周完成
- ✅ 完成X个组件测试
- ✅ 新增X个测试用例
- ✅ 测试覆盖率提升到X%

## 下周计划
- [ ] 完成X个组件测试
- [ ] 修复已知问题
- [ ] 提升测试覆盖率

## 遇到的问题
- 问题描述
- 解决方案
```

---

## 🎯 质量标准

### 测试质量要求
- **覆盖率**: 每个组件 > 80%
- **用例数**: 每个组件 15-35 个
- **断言**: 每个测试至少1个有效断言
- **独立性**: 测试间不互相依赖
- **可维护**: 代码清晰，注释完整

### 通过标准
- ✅ 所有测试通过
- ✅ 无console错误
- ✅ 覆盖率达标
- ✅ 代码审查通过

---

**更新时间**: 2025-11-24  
**预计完成**: 2025-12-10 (15个工作日)  
**负责人**: 待分配
